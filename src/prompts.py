"""送給 Gemini 的 prompt 與字幕語言——**這是資料，不是介面文字，永不翻譯**。

## 為什麼不進 i18n

這裡的字串會被內插進 prompt 送給外部程式（Gemini），直接決定**字幕檔的內容**。
字幕跟著**影片音訊**走：使用者把介面切成日文，字幕不會、也不該跟著變成日文。
把它接上介面語言，等於使用者換個介面配色就把辨識結果整個換掉。

判斷準則（general.md）：這個字串會不會被寫進檔案、或送出去給別的程式？
會 → 資料，永不翻。prompt 的每一個字都會影響輸出，連標點都不能動。

## DEFAULT_TARGET_LANGUAGE 的已知缺陷

它現在是中文字面「繁體中文」而不是機器碼（`zh-TW`）。因為目前它從不落檔、
也不跟任何檔案裡的值比對，所以還沒造成污染。但**日後若做成使用者可選的設定
並存進 .tool_config.json，必須存機器碼、顯示名另走 _display() / _stored()
一對函式**（general.md 的「存進設定檔的預設分類名被翻譯」案例），
不可以直接把「繁體中文」這串存進設定檔。詳見 docs/TODO.md。
"""

from __future__ import annotations

# 字幕（辨識輸出）的目標語言。**與介面語言無關**，見上方說明。
DEFAULT_TARGET_LANGUAGE = "繁體中文"

# ⚠ 從 translator.translate_segment() 原封不動搬過來，字面逐字不變（含縮排與
# 空行）。改動任何一個字都可能改變 Gemini 的輸出，屬於行為變更不是翻譯。
TRANSLATE_PROMPT = """
    請聽這段音訊，將其內容翻譯為 {target_language}，並輸出標準 SRT 字幕格式。

    輸出格式：JSON 格式，包含欄位 "srt_content"。僅輸出 JSON，不附任何說明。

    時間軸規則（最高優先級）：
    1. 所有時間戳必須使用 HH:MM:SS,mmm 格式（例如 00:20:37,340），小時位絕對不可省略。
    2. 時間軸從 00:00:00,000 起算，對應音訊的絕對起點。
    3. 每條字幕的結束時間必須早於下一條字幕的開始時間（嚴格遞增，不可重疊）。
    4. 靜默段不需輸出字幕，但下一條字幕的時間戳必須反映真實的音訊位置。
    5. 每條字幕的顯示時間最長不可超過 5 秒（結束時間 - 開始時間 ≤ 5 秒）。若說話內容超過 5 秒，必須拆分為多條字幕。

    內容規則：
    1. 每行字幕最多 15 個中文字，過長請斷句或拆分為多個字幕塊。
    2. 有人聲說話時，無論清晰或模糊，都必須盡力辨識並翻譯，結合前後文補全語意。
    3. 僅在「完全沒有人聲」的片段（純音樂、純雜音、非語言情緒音、呻吟聲、笑聲、哭聲）才略過，不輸出該段字幕。
    """
